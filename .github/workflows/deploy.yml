name: 🚀 Deploy Trading Data Terminal

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

env:
  BASE_PATH: "/trading-data/"

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
      - name: 📥 Загрузка репозитория
        uses: actions/checkout@v4

      - name: 🟢 Установка Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: 🔐 Проверка GitHub Secrets
        env:
          FIREBASE_API_KEY: ${{ secrets.FIREBASE_API_KEY }}
          FIREBASE_AUTH_DOMAIN: ${{ secrets.FIREBASE_AUTH_DOMAIN }}
          FIREBASE_DATABASE_URL: ${{ secrets.FIREBASE_DATABASE_URL }}
          FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
          FIREBASE_STORAGE_BUCKET: ${{ secrets.FIREBASE_STORAGE_BUCKET }}
          FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}
          FIREBASE_APP_ID: ${{ secrets.FIREBASE_APP_ID }}
          FIREBASE_MEASUREMENT_ID: ${{ secrets.FIREBASE_MEASUREMENT_ID }}
        run: |
          echo "🔐 Проверяем все секреты..."
          [ -z "$FIREBASE_API_KEY" ] && echo "❌ FIREBASE_API_KEY отсутствует" && exit 1
          [ -z "$FIREBASE_AUTH_DOMAIN" ] && echo "❌ FIREBASE_AUTH_DOMAIN отсутствует" && exit 1
          [ -z "$FIREBASE_DATABASE_URL" ] && echo "❌ FIREBASE_DATABASE_URL отсутствует" && exit 1
          [ -z "$FIREBASE_PROJECT_ID" ] && echo "❌ FIREBASE_PROJECT_ID отсутствует" && exit 1
          [ -z "$FIREBASE_STORAGE_BUCKET" ] && echo "❌ FIREBASE_STORAGE_BUCKET отсутствует" && exit 1
          [ -z "$FIREBASE_MESSAGING_SENDER_ID" ] && echo "❌ FIREBASE_MESSAGING_SENDER_ID отсутствует" && exit 1
          [ -z "$FIREBASE_APP_ID" ] && echo "❌ FIREBASE_APP_ID отсутствует" && exit 1
          [ -z "$FIREBASE_MEASUREMENT_ID" ] && echo "❌ FIREBASE_MEASUREMENT_ID отсутствует" && exit 1
          echo "✅ Все секреты присутствуют"

      - name: 🔧 Генерация firebase-config.js
        env:
          FIREBASE_API_KEY: ${{ secrets.FIREBASE_API_KEY }}
          FIREBASE_AUTH_DOMAIN: ${{ secrets.FIREBASE_AUTH_DOMAIN }}
          FIREBASE_DATABASE_URL: ${{ secrets.FIREBASE_DATABASE_URL }}
          FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
          FIREBASE_STORAGE_BUCKET: ${{ secrets.FIREBASE_STORAGE_BUCKET }}
          FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}
          FIREBASE_APP_ID: ${{ secrets.FIREBASE_APP_ID }}
          FIREBASE_MEASUREMENT_ID: ${{ secrets.FIREBASE_MEASUREMENT_ID }}
        run: node scripts/generate-config.js

      - name: 📄 Создание служебных файлов
        run: |
          touch .nojekyll
          echo "askhat707.github.io" > CNAME

      - name: 🔍 Проверка структуры
        run: |
          echo "✅ index.html" && test -f "index.html" || exit 1
          echo "✅ firebase-config.js" && test -f "firebase-config.js" || exit 1
          echo "✅ js/app.js" && test -f "js/app.js" || exit 1
          echo "✅ css/base.css" && test -f "css/base.css" || exit 1

      - name: 📦 Упаковка артефакта
        uses: actions/upload-pages-artifact@v3
        with:
          path: .
          retention-days: 30

      - name: 🚀 Деплой на GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

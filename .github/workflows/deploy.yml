name: 🚀 Deploy Trading Data Terminal

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: 🔐 Validate Secrets
        env:
          FIREBASE_API_KEY: ${{ secrets.FIREBASE_API_KEY }}
          FIREBASE_AUTH_DOMAIN: ${{ secrets.FIREBASE_AUTH_DOMAIN }}
          FIREBASE_DATABASE_URL: ${{ secrets.FIREBASE_DATABASE_URL }}
          FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
          FIREBASE_STORAGE_BUCKET: ${{ secrets.FIREBASE_STORAGE_BUCKET }}
          FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}
          FIREBASE_APP_ID: ${{ secrets.FIREBASE_APP_ID }}
          FIREBASE_MEASUREMENT_ID: ${{ secrets.FIREBASE_MEASUREMENT_ID }}
        run: |
          echo "🔐 [SECRETS] Проверяем все секреты..."
          
          if [ -z "$FIREBASE_API_KEY" ]; then echo "❌ FIREBASE_API_KEY отсутствует"; exit 1; fi
          if [ -z "$FIREBASE_AUTH_DOMAIN" ]; then echo "❌ FIREBASE_AUTH_DOMAIN отсутствует"; exit 1; fi
          if [ -z "$FIREBASE_DATABASE_URL" ]; then echo "❌ FIREBASE_DATABASE_URL отсутствует"; exit 1; fi
          if [ -z "$FIREBASE_PROJECT_ID" ]; then echo "❌ FIREBASE_PROJECT_ID отсутствует"; exit 1; fi
          if [ -z "$FIREBASE_STORAGE_BUCKET" ]; then echo "❌ FIREBASE_STORAGE_BUCKET отсутствует"; exit 1; fi
          if [ -z "$FIREBASE_MESSAGING_SENDER_ID" ]; then echo "❌ FIREBASE_MESSAGING_SENDER_ID отсутствует"; exit 1; fi
          if [ -z "$FIREBASE_APP_ID" ]; then echo "❌ FIREBASE_APP_ID отсутствует"; exit 1; fi
          if [ -z "$FIREBASE_MEASUREMENT_ID" ]; then echo "❌ FIREBASE_MEASUREMENT_ID отсутствует"; exit 1; fi
          
          echo "✅ Все 8 секретов присутствуют!"

      - name: 🧹 Clear GitHub Pages Cache
        run: |
          echo "🧹 [CACHE] Очищаем кэш GitHub Pages..."
          echo "timestamp=$(date +%s)" >> $GITHUB_OUTPUT

      - name: 🔧 Generate Firebase Config with Cache Busting
        env:
          FIREBASE_API_KEY: ${{ secrets.FIREBASE_API_KEY }}
          FIREBASE_AUTH_DOMAIN: ${{ secrets.FIREBASE_AUTH_DOMAIN }}
          FIREBASE_DATABASE_URL: ${{ secrets.FIREBASE_DATABASE_URL }}
          FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
          FIREBASE_STORAGE_BUCKET: ${{ secrets.FIREBASE_STORAGE_BUCKET }}
          FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}
          FIREBASE_APP_ID: ${{ secrets.FIREBASE_APP_ID }}
          FIREBASE_MEASUREMENT_ID: ${{ secrets.FIREBASE_MEASUREMENT_ID }}
        run: |
          echo "🔧 Генерируем firebase-config.js..."
          
          # Создаем конфиг с реальными значениями из Secrets с использованием простого метода
          echo "// ====================================================" > firebase-config.js
          echo "// 🔥 FIREBASE CONFIG - GENERATED FROM GITHUB SECRETS" >> firebase-config.js
          echo "// ⚠️ Этот файл создается автоматически GitHub Actions" >> firebase-config.js
          echo "// ====================================================" >> firebase-config.js
          echo "" >> firebase-config.js
          echo "console.log('🚀 Загрузка Firebase конфигурации...');" >> firebase-config.js
          echo "" >> firebase-config.js
          echo "const firebaseConfig = {" >> firebase-config.js
          echo "  apiKey: \"$FIREBASE_API_KEY\"," >> firebase-config.js
          echo "  authDomain: \"$FIREBASE_AUTH_DOMAIN\"," >> firebase-config.js
          echo "  databaseURL: \"$FIREBASE_DATABASE_URL\"," >> firebase-config.js
          echo "  projectId: \"$FIREBASE_PROJECT_ID\"," >> firebase-config.js
          echo "  storageBucket: \"$FIREBASE_STORAGE_BUCKET\"," >> firebase-config.js
          echo "  messagingSenderId: \"$FIREBASE_MESSAGING_SENDER_ID\"," >> firebase-config.js
          echo "  appId: \"$FIREBASE_APP_ID\"," >> firebase-config.js
          echo "  measurementId: \"$FIREBASE_MEASUREMENT_ID\"" >> firebase-config.js
          echo "};" >> firebase-config.js
          echo "" >> firebase-config.js
          echo "console.log('✅ [CONFIG] Конфигурация готова:', firebaseConfig.projectId);" >> firebase-config.js
          echo "" >> firebase-config.js
          echo "// Сохраняем в глобальной области видимости" >> firebase-config.js
          echo "if (typeof window !== 'undefined') {" >> firebase-config.js
          echo "  window.firebaseConfig = firebaseConfig;" >> firebase-config.js
          echo "  console.log('✅ [CONFIG] firebaseConfig доступен в window');" >> firebase-config.js
          echo "}" >> firebase-config.js
          echo "" >> firebase-config.js
          echo "// CommonJS экспорт" >> firebase-config.js
          echo "if (typeof module !== 'undefined' && module.exports) {" >> firebase-config.js
          echo "  module.exports = firebaseConfig;" >> firebase-config.js
          echo "}" >> firebase-config.js
          
          echo "✅ Конфиг создан"
          echo "📏 Размер файла:" $(wc -c < firebase-config.js) "байт"
          echo "🔍 Проверка конфига..."
          
          # Проверяем, что нет плейсхолдеров
          if grep -q "{{" firebase-config.js; then
            echo "❌ ОШИБКА: В конфиге остались плейсхолдеры!"
            cat firebase-config.js
            exit 1
          fi
          
          if grep -q "FIREBASE_API_KEY" firebase-config.js; then
            echo "❌ ОШИБКА: Переменные не подставлены!"
            cat firebase-config.js
            exit 1
          fi
          
          echo "✅ Конфиг проверен - все переменные подставлены"
          
          # Удаляем шаблонный файл, чтобы не было путаницы
          if [ -f "firebase-config.js.template" ]; then
            echo "🗑️ Удаляем шаблонный файл..."
            rm firebase-config.js.template
          fi

      - name: 📄 Create Support Files
        run: |
          touch .nojekyll
          echo "askhat707.github.io" > CNAME
          echo "✅ Файлы созданы"

      - name: 📋 List Files
        run: |
          echo "📁 Структура репозитория:"
          ls -la | head -20
          echo ""
          echo "✅ Критические файлы:"
          test -f "index.html" && echo "  ✅ index.html" || echo "  ❌ index.html"
          test -f "firebase-config.js" && echo "  ✅ firebase-config.js" || echo "  ❌ firebase-config.js"
          test -f "js/app.js" && echo "  ✅ js/app.js" || echo "  ❌ js/app.js"
          test -f "css/base.css" && echo "  ✅ css/base.css" || echo "  ❌ css/base.css"
          echo ""
          echo "🔍 Проверка firebase-config.js:"
          if [ -f "firebase-config.js" ]; then
            echo "Первые 10 строк:"
            head -10 firebase-config.js
          fi

      - name: 📦 Upload Pages Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: .

      - name: 🚀 Deploy to Pages
        id: deployment
        uses: actions/deploy-pages@v4

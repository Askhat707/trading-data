name: 🚀 Deploy Trading Data Terminal

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: 🔐 Validate Secrets
        env:
          FIREBASE_API_KEY: ${{ secrets.FIREBASE_API_KEY }}
          FIREBASE_AUTH_DOMAIN: ${{ secrets.FIREBASE_AUTH_DOMAIN }}
          FIREBASE_DATABASE_URL: ${{ secrets.FIREBASE_DATABASE_URL }}
          FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
          FIREBASE_STORAGE_BUCKET: ${{ secrets.FIREBASE_STORAGE_BUCKET }}
          FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}
          FIREBASE_APP_ID: ${{ secrets.FIREBASE_APP_ID }}
          FIREBASE_MEASUREMENT_ID: ${{ secrets.FIREBASE_MEASUREMENT_ID }}
        run: |
          echo "🔐 [SECRETS] Проверяем все секреты..."
          
          if [ -z "$FIREBASE_API_KEY" ]; then echo "❌ FIREBASE_API_KEY отсутствует"; exit 1; fi
          if [ -z "$FIREBASE_AUTH_DOMAIN" ]; then echo "❌ FIREBASE_AUTH_DOMAIN отсутствует"; exit 1; fi
          if [ -z "$FIREBASE_DATABASE_URL" ]; then echo "❌ FIREBASE_DATABASE_URL отсутствует"; exit 1; fi
          if [ -z "$FIREBASE_PROJECT_ID" ]; then echo "❌ FIREBASE_PROJECT_ID отсутствует"; exit 1; fi
          if [ -z "$FIREBASE_STORAGE_BUCKET" ]; then echo "❌ FIREBASE_STORAGE_BUCKET отсутствует"; exit 1; fi
          if [ -z "$FIREBASE_MESSAGING_SENDER_ID" ]; then echo "❌ FIREBASE_MESSAGING_SENDER_ID отсутствует"; exit 1; fi
          if [ -z "$FIREBASE_APP_ID" ]; then echo "❌ FIREBASE_APP_ID отсутствует"; exit 1; fi
          if [ -z "$FIREBASE_MEASUREMENT_ID" ]; then echo "❌ FIREBASE_MEASUREMENT_ID отсутствует"; exit 1; fi
          
          echo "✅ Все 8 секретов присутствуют!"

      # --- НАЧАЛО ВСТАВКИ ВАШЕГО КОДА ---
      - name: 🧹 Clear GitHub Pages Cache
        run: |
          echo "🧹 [CACHE] Очищаем кэш GitHub Pages..."
          echo "timestamp=$(date +%s)" >> $GITHUB_OUTPUT

      - name: 🔧 Generate Firebase Config with Cache Busting
        env:
          FIREBASE_API_KEY: ${{ secrets.FIREBASE_API_KEY }}
          FIREBASE_AUTH_DOMAIN: ${{ secrets.FIREBASE_AUTH_DOMAIN }}
          FIREBASE_DATABASE_URL: ${{ secrets.FIREBASE_DATABASE_URL }}
          FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
          FIREBASE_STORAGE_BUCKET: ${{ secrets.FIREBASE_STORAGE_BUCKET }}
          FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}
          FIREBASE_APP_ID: ${{ secrets.FIREBASE_APP_ID }}
          FIREBASE_MEASUREMENT_ID: ${{ secrets.FIREBASE_MEASUREMENT_ID }}
        run: node scripts/generate-config.js
      # --- КОНЕЦ ВСТАВКИ ВАШЕГО КОДА ---

      - name: 📄 Create Support Files
        run: |
          touch .nojekyll
          echo "askhat707.github.io" > CNAME
          echo "✅ Файлы созданы"

      - name: 📋 List Files
        run: |
          echo "📁 Структура репозитория:"
          ls -la | head -20
          echo ""
          echo "✅ Критические файлы:"
          test -f "index.html" && echo "  ✅ index.html" || echo "  ❌ index.html"
          test -f "firebase-config.js" && echo "  ✅ firebase-config.js" || echo "  ❌ firebase-config.js"
          test -f "js/app.js" && echo "  ✅ js/app.js" || echo "  ❌ js/app.js"
          test -f "css/base.css" && echo "  ✅ css/base.css" || echo "  ❌ css/base.css"

      - name: 📦 Upload Pages Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: .

      - name: 🚀 Deploy to Pages
        id: deployment
        uses: actions/deploy-pages@v4

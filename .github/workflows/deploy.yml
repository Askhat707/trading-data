name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Generate Firebase Config from Secrets
        run: |
          cat > firebase-config.js << 'EOF'
          // 🔥 FIREBASE CONFIG - СГЕНЕРИРОВАН ИЗ GITHUB SECRETS
          const firebaseConfig = {
            apiKey: "${{ secrets.FIREBASE_API_KEY }}",
            authDomain: "${{ secrets.FIREBASE_AUTH_DOMAIN }}",
            databaseURL: "${{ secrets.FIREBASE_DATABASE_URL }}",
            projectId: "${{ secrets.FIREBASE_PROJECT_ID }}",
            storageBucket: "${{ secrets.FIREBASE_STORAGE_BUCKET }}",
            messagingSenderId: "${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}",
            appId: "${{ secrets.FIREBASE_APP_ID }}",
            measurementId: "${{ secrets.FIREBASE_MEASUREMENT_ID }}"
          };
          
          // Экспорт для глобального использования
          window.firebaseConfig = firebaseConfig;
          
          console.log('✅ Firebase конфиг загружен из GitHub Secrets');
          EOF
          
          echo "✅ firebase-config.js создан"
          head -20 firebase-config.js
      
      - name: Setup Pages
        uses: actions/configure-pages@v3
      
      - name: Build and copy files
        run: |
          mkdir -p _site
          cp -r index.html _site/
          cp -r firebase-config.js _site/
          cp -r service-worker.js _site/
          cp -r css/ _site/
          cp -r js/ _site/
          cp -r scripts/ _site/
          
          echo "=== Файлы в _site ==="
          ls -la _site/
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v2
        with:
          path: '_site'
      
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2

      - name: Deployment Status
        run: |
          echo "✅ Deployment успешен!"
          echo "🌐 Сайт: https://askhat707.github.io/trading-data/"

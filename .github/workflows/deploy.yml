name: 🚀 Deploy Trading Data Terminal

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 🔐 Validate GitHub Secrets
        env:
          FIREBASE_API_KEY: ${{ secrets.FIREBASE_API_KEY }}
          FIREBASE_AUTH_DOMAIN: ${{ secrets.FIREBASE_AUTH_DOMAIN }}
          FIREBASE_DATABASE_URL: ${{ secrets.FIREBASE_DATABASE_URL }}
          FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
          FIREBASE_STORAGE_BUCKET: ${{ secrets.FIREBASE_STORAGE_BUCKET }}
          FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}
          FIREBASE_APP_ID: ${{ secrets.FIREBASE_APP_ID }}
          FIREBASE_MEASUREMENT_ID: ${{ secrets.FIREBASE_MEASUREMENT_ID }}
        run: |
          echo "🔐 Проверка Firebase Secrets..."
          
          if [ -z "$FIREBASE_API_KEY" ]; then
            echo "❌ FIREBASE_API_KEY не установлен"
            exit 1
          fi
          if [ -z "$FIREBASE_PROJECT_ID" ]; then
            echo "❌ FIREBASE_PROJECT_ID не установлен"
            exit 1
          fi
          
          echo "✅ ВСЕ СЕКРЕТЫ ПРИСУТСТВУЮТ"

      - name: 🔧 Generate Firebase Config
        env:
          FIREBASE_API_KEY: ${{ secrets.FIREBASE_API_KEY }}
          FIREBASE_AUTH_DOMAIN: ${{ secrets.FIREBASE_AUTH_DOMAIN }}
          FIREBASE_DATABASE_URL: ${{ secrets.FIREBASE_DATABASE_URL }}
          FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
          FIREBASE_STORAGE_BUCKET: ${{ secrets.FIREBASE_STORAGE_BUCKET }}
          FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}
          FIREBASE_APP_ID: ${{ secrets.FIREBASE_APP_ID }}
          FIREBASE_MEASUREMENT_ID: ${{ secrets.FIREBASE_MEASUREMENT_ID }}
        run: |
          echo "🔧 Генерация firebase-config.js..."
          
          cat > firebase-config.js << 'EOF'
          console.log('🚀 Загрузка Firebase конфигурации...');
          
          const firebaseConfig = {
            apiKey: "${FIREBASE_API_KEY}",
            authDomain: "${FIREBASE_AUTH_DOMAIN}",
            databaseURL: "${FIREBASE_DATABASE_URL}",
            projectId: "${FIREBASE_PROJECT_ID}",
            storageBucket: "${FIREBASE_STORAGE_BUCKET}",
            messagingSenderId: "${FIREBASE_MESSAGING_SENDER_ID}",
            appId: "${FIREBASE_APP_ID}",
            measurementId: "${FIREBASE_MEASUREMENT_ID}"
          };
          
          console.log('✅ Firebase Config:', firebaseConfig.projectId);
          window.firebaseConfig = firebaseConfig;
          EOF
          
          echo "✅ Config создан"
          head -3 firebase-config.js

      - name: 📄 Create Support Files
        run: |
          touch .nojekyll
          echo "✅ .nojekyll создан"

      - name: 📁 Verify Structure
        run: |
          echo "📁 Проверка структуры:"
          test -f "index.html" && echo "✅ index.html" || echo "❌ index.html отсутствует"
          test -f "firebase-config.js" && echo "✅ firebase-config.js" || echo "❌ firebase-config.js отсутствует"
          test -f ".nojekyll" && echo "✅ .nojekyll" || echo "❌ .nojekyll отсутствует"
          ls -la js/modules/auth.js

      - name: 📦 Upload to Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: .

      - name: 🚀 Deploy
        id: deployment
        uses: actions/deploy-pages@v4
